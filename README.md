# IssuePilot
ssuePilot is an open-source GitHub bot that automatically triages issues, detects duplicates, applies labels, manages stale discussions, checks dependencies, and generates release notes.
